# 时间复制性

> 如何度量？f(n)，n为输入串的长度，同时考虑最坏/最好情况

- 最坏情况-特定输入运行最长时间
- 时间？步数

- 大O，小o记号
  - BIG-O：
  - small-O：严格小于

- 普通多项式一定比对数增长的快
$$\log ^{100}n = o(n^{0.1})$$
- 指数函数一定比普通多项式增长快
$$n^{100} = o(1.01^{n})$$

- $\mathbf{TIME}(t(n))$：所以能在$O{t(n)}$时间内判定的集合

## 复杂性理论 and 可计算理论

- 不同模型处理问题，时间复杂度不一样，使用哪种模型度量时间？

## Class P

### 多项式时间

- 确定的TM,多项式时间可以求解的问题

- 证明算法在多项式时间内运行
